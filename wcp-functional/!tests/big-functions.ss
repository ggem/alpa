;

(define (big-functions x)
  (fun00 (+ x 2) (- x 3) 5))

(define (fun00 i j k)
  (if (fun20 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun04 j k i)
        (fun99 k i j))))

(define (fun01 i j k)
  (if (fun30 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun24 j k i)
        (fun67 k i j))))

(define (fun02 i j k)
  (if (fun53 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun90 j k i)
        (fun00 k i j))))

(define (fun03 i j k)
  (if (fun90 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun55 j k i)
        (fun15 k i j))))

(define (fun04 i j k)
  (if (fun43 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun69 j k i)
        (fun84 k i j))))

(define (fun05 i j k)
  (if (fun29 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun51 j k i)
        (fun25 k i j))))

(define (fun06 i j k)
  (if (fun62 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun35 j k i)
        (fun54 k i j))))

(define (fun07 i j k)
  (if (fun70 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun53 j k i)
        (fun38 k i j))))

(define (fun08 i j k)
  (if (fun00 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun85 j k i)
        (fun74 k i j))))

(define (fun09 i j k)
  (if (fun54 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun83 j k i)
        (fun10 k i j))))

(define (fun10 i j k)
  (if (fun68 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun73 j k i)
        (fun33 k i j))))

(define (fun11 i j k)
  (if (fun50 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun32 j k i)
        (fun72 k i j))))

(define (fun12 i j k)
  (if (fun56 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun65 j k i)
        (fun81 k i j))))

(define (fun13 i j k)
  (if (fun69 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun21 j k i)
        (fun55 k i j))))

(define (fun14 i j k)
  (if (fun33 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun30 j k i)
        (fun51 k i j))))

(define (fun15 i j k)
  (if (fun61 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun13 j k i)
        (fun92 k i j))))

(define (fun16 i j k)
  (if (fun24 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun86 j k i)
        (fun07 k i j))))

(define (fun17 i j k)
  (if (fun06 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun92 j k i)
        (fun57 k i j))))

(define (fun18 i j k)
  (if (fun16 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun07 j k i)
        (fun14 k i j))))

(define (fun19 i j k)
  (if (fun39 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun67 j k i)
        (fun45 k i j))))

(define (fun20 i j k)
  (if (fun91 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun15 j k i)
        (fun95 k i j))))

(define (fun21 i j k)
  (if (fun08 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun38 j k i)
        (fun19 k i j))))

(define (fun22 i j k)
  (if (fun14 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun33 j k i)
        (fun79 k i j))))

(define (fun23 i j k)
  (if (fun58 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun81 j k i)
        (fun06 k i j))))

(define (fun24 i j k)
  (if (fun66 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun06 j k i)
        (fun86 k i j))))

(define (fun25 i j k)
  (if (fun03 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun16 j k i)
        (fun27 k i j))))

(define (fun26 i j k)
  (if (fun64 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun37 j k i)
        (fun78 k i j))))

(define (fun27 i j k)
  (if (fun25 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun84 j k i)
        (fun01 k i j))))

(define (fun28 i j k)
  (if (fun01 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun43 j k i)
        (fun42 k i j))))

(define (fun29 i j k)
  (if (fun18 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun18 j k i)
        (fun28 k i j))))

(define (fun30 i j k)
  (if (fun40 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun58 j k i)
        (fun32 k i j))))

(define (fun31 i j k)
  (if (fun12 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun94 j k i)
        (fun39 k i j))))

(define (fun32 i j k)
  (if (fun48 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun93 j k i)
        (fun91 k i j))))

(define (fun33 i j k)
  (if (fun79 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun12 j k i)
        (fun93 k i j))))

(define (fun34 i j k)
  (if (fun51 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun60 j k i)
        (fun48 k i j))))

(define (fun35 i j k)
  (if (fun95 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun10 j k i)
        (fun76 k i j))))

(define (fun36 i j k)
  (if (fun42 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun14 j k i)
        (fun97 k i j))))

(define (fun37 i j k)
  (if (fun80 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun36 j k i)
        (fun90 k i j))))

(define (fun38 i j k)
  (if (fun94 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun02 j k i)
        (fun18 k i j))))

(define (fun39 i j k)
  (if (fun78 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun74 j k i)
        (fun22 k i j))))

(define (fun40 i j k)
  (if (fun55 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun09 j k i)
        (fun26 k i j))))

(define (fun41 i j k)
  (if (fun73 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun61 j k i)
        (fun61 k i j))))

(define (fun42 i j k)
  (if (fun98 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun20 j k i)
        (fun87 k i j))))

(define (fun43 i j k)
  (if (fun38 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun80 j k i)
        (fun49 k i j))))

(define (fun44 i j k)
  (if (fun32 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun99 j k i)
        (fun73 k i j))))

(define (fun45 i j k)
  (if (fun46 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun41 j k i)
        (fun20 k i j))))

(define (fun46 i j k)
  (if (fun36 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun40 j k i)
        (fun17 k i j))))

(define (fun47 i j k)
  (if (fun87 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun52 j k i)
        (fun88 k i j))))

(define (fun48 i j k)
  (if (fun67 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun00 j k i)
        (fun69 k i j))))

(define (fun49 i j k)
  (if (fun19 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun26 j k i)
        (fun11 k i j))))

(define (fun50 i j k)
  (if (fun21 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun87 j k i)
        (fun47 k i j))))

(define (fun51 i j k)
  (if (fun28 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun47 j k i)
        (fun41 k i j))))

(define (fun52 i j k)
  (if (fun23 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun62 j k i)
        (fun35 k i j))))

(define (fun53 i j k)
  (if (fun74 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun50 j k i)
        (fun64 k i j))))

(define (fun54 i j k)
  (if (fun45 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun44 j k i)
        (fun30 k i j))))

(define (fun55 i j k)
  (if (fun27 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun19 j k i)
        (fun46 k i j))))

(define (fun56 i j k)
  (if (fun47 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun17 j k i)
        (fun50 k i j))))

(define (fun57 i j k)
  (if (fun41 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun27 j k i)
        (fun16 k i j))))

(define (fun58 i j k)
  (if (fun60 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun25 j k i)
        (fun21 k i j))))

(define (fun59 i j k)
  (if (fun83 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun28 j k i)
        (fun94 k i j))))

(define (fun60 i j k)
  (if (fun31 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun03 j k i)
        (fun37 k i j))))

(define (fun61 i j k)
  (if (fun07 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun42 j k i)
        (fun43 k i j))))

(define (fun62 i j k)
  (if (fun22 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun77 j k i)
        (fun60 k i j))))

(define (fun63 i j k)
  (if (fun49 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun11 j k i)
        (fun96 k i j))))

(define (fun64 i j k)
  (if (fun81 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun01 j k i)
        (fun40 k i j))))

(define (fun65 i j k)
  (if (fun97 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun29 j k i)
        (fun89 k i j))))

(define (fun66 i j k)
  (if (fun99 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun54 j k i)
        (fun03 k i j))))

(define (fun67 i j k)
  (if (fun65 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun64 j k i)
        (fun66 k i j))))

(define (fun68 i j k)
  (if (fun37 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun91 j k i)
        (fun29 k i j))))

(define (fun69 i j k)
  (if (fun35 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun23 j k i)
        (fun65 k i j))))

(define (fun70 i j k)
  (if (fun76 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun39 j k i)
        (fun83 k i j))))

(define (fun71 i j k)
  (if (fun72 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun68 j k i)
        (fun56 k i j))))

(define (fun72 i j k)
  (if (fun85 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun56 j k i)
        (fun34 k i j))))

(define (fun73 i j k)
  (if (fun59 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun59 j k i)
        (fun75 k i j))))

(define (fun74 i j k)
  (if (fun96 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun79 j k i)
        (fun02 k i j))))

(define (fun75 i j k)
  (if (fun44 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun97 j k i)
        (fun98 k i j))))

(define (fun76 i j k)
  (if (fun04 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun95 j k i)
        (fun62 k i j))))

(define (fun77 i j k)
  (if (fun26 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun46 j k i)
        (fun08 k i j))))

(define (fun78 i j k)
  (if (fun34 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun63 j k i)
        (fun05 k i j))))

(define (fun79 i j k)
  (if (fun11 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun22 j k i)
        (fun31 k i j))))

(define (fun80 i j k)
  (if (fun77 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun34 j k i)
        (fun68 k i j))))

(define (fun81 i j k)
  (if (fun84 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun88 j k i)
        (fun70 k i j))))

(define (fun82 i j k)
  (if (fun02 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun96 j k i)
        (fun71 k i j))))

(define (fun83 i j k)
  (if (fun57 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun71 j k i)
        (fun82 k i j))))

(define (fun84 i j k)
  (if (fun82 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun82 j k i)
        (fun52 k i j))))

(define (fun85 i j k)
  (if (fun17 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun75 j k i)
        (fun77 k i j))))

(define (fun86 i j k)
  (if (fun52 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun70 j k i)
        (fun59 k i j))))

(define (fun87 i j k)
  (if (fun13 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun49 j k i)
        (fun24 k i j))))

(define (fun88 i j k)
  (if (fun86 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun08 j k i)
        (fun44 k i j))))

(define (fun89 i j k)
  (if (fun09 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun98 j k i)
        (fun13 k i j))))

(define (fun90 i j k)
  (if (fun05 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun57 j k i)
        (fun63 k i j))))

(define (fun91 i j k)
  (if (fun10 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun31 j k i)
        (fun53 k i j))))

(define (fun92 i j k)
  (if (fun71 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun48 j k i)
        (fun36 k i j))))

(define (fun93 i j k)
  (if (fun75 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun76 j k i)
        (fun80 k i j))))

(define (fun94 i j k)
  (if (fun93 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun45 j k i)
        (fun09 k i j))))

(define (fun95 i j k)
  (if (fun15 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun78 j k i)
        (fun58 k i j))))

(define (fun96 i j k)
  (if (fun88 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun72 j k i)
        (fun23 k i j))))

(define (fun97 i j k)
  (if (fun92 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun66 j k i)
        (fun04 k i j))))

(define (fun98 i j k)
  (if (fun89 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun89 j k i)
        (fun12 k i j))))

(define (fun99 i j k)
  (if (fun63 i j k)
      '()
      (append
        (cons i (cons j (cons k '())))
        (fun05 j k i)
        (fun85 k i j))))

'(big-functions size)