;;
;;

(define (index item ls)
  (f00 item ls 0))

(define (f00 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f01 item (cdr ls) (+ count 1)))))
(define (f01 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f02 item (cdr ls) (+ count 1)))))
(define (f02 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f03 item (cdr ls) (+ count 1)))))
(define (f03 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f04 item (cdr ls) (+ count 1)))))
(define (f04 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f05 item (cdr ls) (+ count 1)))))
(define (f05 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f06 item (cdr ls) (+ count 1)))))
(define (f06 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f07 item (cdr ls) (+ count 1)))))
(define (f07 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f08 item (cdr ls) (+ count 1)))))
(define (f08 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f09 item (cdr ls) (+ count 1)))))
(define (f09 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f10 item (cdr ls) (+ count 1)))))
(define (f10 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f11 item (cdr ls) (+ count 1)))))
(define (f11 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f12 item (cdr ls) (+ count 1)))))
(define (f12 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f13 item (cdr ls) (+ count 1)))))
(define (f13 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f14 item (cdr ls) (+ count 1)))))
(define (f14 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f15 item (cdr ls) (+ count 1)))))
(define (f15 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f16 item (cdr ls) (+ count 1)))))
(define (f16 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f17 item (cdr ls) (+ count 1)))))
(define (f17 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f18 item (cdr ls) (+ count 1)))))
(define (f18 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f19 item (cdr ls) (+ count 1)))))
(define (f19 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f20 item (cdr ls) (+ count 1)))))
(define (f20 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f21 item (cdr ls) (+ count 1)))))
(define (f21 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f22 item (cdr ls) (+ count 1)))))
(define (f22 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f23 item (cdr ls) (+ count 1)))))
(define (f23 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f24 item (cdr ls) (+ count 1)))))
(define (f24 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f25 item (cdr ls) (+ count 1)))))
(define (f25 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f26 item (cdr ls) (+ count 1)))))
(define (f26 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f27 item (cdr ls) (+ count 1)))))
(define (f27 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f28 item (cdr ls) (+ count 1)))))
(define (f28 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f29 item (cdr ls) (+ count 1)))))
(define (f29 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f30 item (cdr ls) (+ count 1)))))
(define (f30 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f31 item (cdr ls) (+ count 1)))))
(define (f31 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f32 item (cdr ls) (+ count 1)))))
(define (f32 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f33 item (cdr ls) (+ count 1)))))
(define (f33 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f34 item (cdr ls) (+ count 1)))))
(define (f34 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f35 item (cdr ls) (+ count 1)))))
(define (f35 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f36 item (cdr ls) (+ count 1)))))
(define (f36 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f37 item (cdr ls) (+ count 1)))))
(define (f37 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f38 item (cdr ls) (+ count 1)))))
(define (f38 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f39 item (cdr ls) (+ count 1)))))
(define (f39 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f40 item (cdr ls) (+ count 1)))))
(define (f40 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f41 item (cdr ls) (+ count 1)))))
(define (f41 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f42 item (cdr ls) (+ count 1)))))
(define (f42 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f43 item (cdr ls) (+ count 1)))))
(define (f43 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f44 item (cdr ls) (+ count 1)))))
(define (f44 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f45 item (cdr ls) (+ count 1)))))
(define (f45 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f46 item (cdr ls) (+ count 1)))))
(define (f46 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f47 item (cdr ls) (+ count 1)))))
(define (f47 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f48 item (cdr ls) (+ count 1)))))
(define (f48 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f49 item (cdr ls) (+ count 1)))))
(define (f49 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f50 item (cdr ls) (+ count 1)))))
(define (f50 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f51 item (cdr ls) (+ count 1)))))
(define (f51 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f52 item (cdr ls) (+ count 1)))))
(define (f52 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f53 item (cdr ls) (+ count 1)))))
(define (f53 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f54 item (cdr ls) (+ count 1)))))
(define (f54 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f55 item (cdr ls) (+ count 1)))))
(define (f55 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f56 item (cdr ls) (+ count 1)))))
(define (f56 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f57 item (cdr ls) (+ count 1)))))
(define (f57 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f58 item (cdr ls) (+ count 1)))))
(define (f58 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f59 item (cdr ls) (+ count 1)))))
(define (f59 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f60 item (cdr ls) (+ count 1)))))
(define (f60 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f61 item (cdr ls) (+ count 1)))))
(define (f61 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f62 item (cdr ls) (+ count 1)))))
(define (f62 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f63 item (cdr ls) (+ count 1)))))
(define (f63 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f64 item (cdr ls) (+ count 1)))))
(define (f64 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f65 item (cdr ls) (+ count 1)))))
(define (f65 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f66 item (cdr ls) (+ count 1)))))
(define (f66 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f67 item (cdr ls) (+ count 1)))))
(define (f67 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f68 item (cdr ls) (+ count 1)))))
(define (f68 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f69 item (cdr ls) (+ count 1)))))
(define (f69 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f70 item (cdr ls) (+ count 1)))))
(define (f70 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f71 item (cdr ls) (+ count 1)))))
(define (f71 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f72 item (cdr ls) (+ count 1)))))
(define (f72 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f73 item (cdr ls) (+ count 1)))))
(define (f73 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f74 item (cdr ls) (+ count 1)))))
(define (f74 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f75 item (cdr ls) (+ count 1)))))
(define (f75 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f76 item (cdr ls) (+ count 1)))))
(define (f76 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f77 item (cdr ls) (+ count 1)))))
(define (f77 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f78 item (cdr ls) (+ count 1)))))
(define (f78 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f79 item (cdr ls) (+ count 1)))))
(define (f79 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f80 item (cdr ls) (+ count 1)))))
(define (f80 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f81 item (cdr ls) (+ count 1)))))
(define (f81 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f82 item (cdr ls) (+ count 1)))))
(define (f82 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f83 item (cdr ls) (+ count 1)))))
(define (f83 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f84 item (cdr ls) (+ count 1)))))
(define (f84 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f85 item (cdr ls) (+ count 1)))))
(define (f85 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f86 item (cdr ls) (+ count 1)))))
(define (f86 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f87 item (cdr ls) (+ count 1)))))
(define (f87 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f88 item (cdr ls) (+ count 1)))))
(define (f88 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f89 item (cdr ls) (+ count 1)))))
(define (f89 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f90 item (cdr ls) (+ count 1)))))
(define (f90 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f91 item (cdr ls) (+ count 1)))))
(define (f91 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f92 item (cdr ls) (+ count 1)))))
(define (f92 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f93 item (cdr ls) (+ count 1)))))
(define (f93 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f94 item (cdr ls) (+ count 1)))))
(define (f94 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f95 item (cdr ls) (+ count 1)))))
(define (f95 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f96 item (cdr ls) (+ count 1)))))
(define (f96 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f97 item (cdr ls) (+ count 1)))))
(define (f97 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f98 item (cdr ls) (+ count 1)))))
(define (f98 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f99 item (cdr ls) (+ count 1)))))
(define (f99 item ls count)
  (if (null? ls)
      -1
      (if (eq? item (car ls))
          count
          (f00 item (cdr ls) (+ count 1))))))

'(index unknown (make-list size 'unknown))
